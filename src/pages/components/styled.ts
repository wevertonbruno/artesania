import styled from "styled-components";

export const Container = styled.section`
  width: 100%;
  section {
    display: flex;
    flex-direction: column;
    margin: 2rem 0;

    div.flex {
      display: flex;
      gap: 1rem;
    }
  }
`;
